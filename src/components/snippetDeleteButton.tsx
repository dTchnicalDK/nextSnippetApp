"use client";
import { Button } from "./ui/button";

const snippetDeleteButton = () => {
  return (
    <div>
      <Button variant="destructive">delete</Button>
    </div>
  );
};

export default snippetDeleteButton;
